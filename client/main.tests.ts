import { mocha } from 'meteor/avital:mocha';
import { chai, assert } from 'meteor/practicalmeteor:chai';

describe('Test', () => {
    it('should just run', () => {
        assert.equal(5, 5);
    });
});
